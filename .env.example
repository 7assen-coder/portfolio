# Optional: raises GitHub REST API rate limit (5000/hr vs 60/hr) for server-side repo fetch.
# Create a fine-grained token with read-only public repo scope; add in Vercel project settings.
GITHUB_TOKEN=
